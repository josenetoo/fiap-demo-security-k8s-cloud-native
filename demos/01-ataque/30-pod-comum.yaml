# Pod sem privilegios explicitos que herda cluster-admin via serviceAccountName.
apiVersion: v1
kind: Pod
metadata:
  name: pod-comum
  namespace: ataque
spec:
  serviceAccountName: app-legado
  containers:
    - name: app
      image: alpine:3.20
      command: ["sleep", "infinity"]
